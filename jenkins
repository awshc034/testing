pipeline {
    agent any
    tools {
        jdk 'java-21'
        maven 'maven'
    }
    stages {
        stage('git checkout') {
            steps {
           git branch: 'main', url: 'https://github.com/awshc034/testing.git'
            }
        }
    stage('compile'){
        steps {
            sh "maven compile"
        }
     }
     stage('build'){
        steps {
            sh "maven clean install"
        }
     }
    }
}

